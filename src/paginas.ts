// Entry point for the static pages (privacy, evidence, who is behind…). They are plain HTML
// built by Vite as extra inputs, so they open without loading the app: whoever is deciding
// whether to trust Glyno should not have to download React and three.js to read a policy.
import '@fontsource-variable/lora'
import '@fontsource-variable/inter'
import './theme.css'
import './paginas.css'
